# klabast/homebrew-tap

Homebrew tap for [klabast](https://github.com/klabast) tools.

```sh
brew tap klabast/tap
brew install mda                        # macOSDefaultApps cli
brew install --cask macosdefaultapps    # macOSDefaultApps app
```

Formulae land here with the first tagged release of each tool.
